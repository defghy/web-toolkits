{
  "name": "@aweb/world",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "ready": "npm install pnpm@9 rimraf@latest -g && npm run bootstrap",
    "bootstrap": "pnpm install",
    "clean": "pnpm --recursive exec -- npx rimraf node_modules && npx rimraf node_modules"
  },
  "peerDependencies": {
    "pnpm": "^9.15.6",
    "rimraf": "3.0.2"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/wicg-file-system-access": "",
    "execa": "^8.0.1",
    "axios": "^1.6.2"
  },
  "engines": {
    "node": ">= 20"
  }
}
